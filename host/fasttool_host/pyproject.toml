[project]
name = "fasttool-host"
version = "0.1.0"
description = "FastCommandCenter's half of the FastTool bridge: load fasttool.json manifests, launch tools, send WM_COPYDATA actions"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = ["PySide6>=6.11.1", "pywin32>=310"]

[dependency-groups]
dev = [
    "ruff>=0.8",
    "mypy>=1.13",
    "pytest>=8.3",
    "types-pywin32",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["fasttool_host"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
files = ["fasttool_host"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
